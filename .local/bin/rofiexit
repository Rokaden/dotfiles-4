#!/bin/sh

esc(){
	dunstify -C 4088991
	exit
}

dunstify -r 4088991 -t 10000 "do you want to logout?" -i "/home/dave/.config/dunst/logout.png"
foo=$(echo "no\nyes" | rofi -dmenu -i)
[ ! "$foo" ] && esc
if [ "$foo" = "yes" ]; then
	i3-msg exit
else
	esc
fi
