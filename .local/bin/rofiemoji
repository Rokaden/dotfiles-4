#!/bin/sh

# basically Luke Smith's emoji script.

chosen=$(grep -v "#" ~/.local/share/emoji | rofi -dmenu -i)
[ ! "$chosen" ] && exit

c=$(echo "$chosen" | sed "s/ .*//")
echo "$c" | tr -d '\n' | xclip -selection clipboard
dunstify -r 4088992 -t 3000 "'$c' copied to clipboard." &
